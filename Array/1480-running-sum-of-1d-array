class Solution {
    public int[] runningSum(int[] nums) {
        int n=nums.length;
        for(int i=1;i<n;i++)
        {
            nums[i]=nums[i]+nums[i-1];
        }
        return nums;
        
    }
}
// In 1d array when the array is created and elemnts are present in that it can be changed by index
// when i=1 nums[i]= some values it stores at ith index that means at it stores that value at i=1
